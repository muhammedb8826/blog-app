<%= form_with model: @post, url: user_posts_path, class: 'post-form' do |f| %>
    <%= f.label :title, 'Title: '%>
    <%= f.text_field :title %>
    <%= f.label :text, 'Text: '%>
    <%= f.text_area :text %>
    <%= f.submit %>
<% end %>